<% fields_for "product[auction_attributes]" do |form| %>
	<%= form.label :początek_aukcji %><br/>
	<%= form.date_select :start %><br/>
	<br/>
	<%= form.label :koniec_aukcji %><br/>
	<%= form.date_select :end %><br/>
	<%= form.label :opis_aukcji%><br/>
	<%= form.text_area :description, :cols => "60", :rows => "20", :class=> 'tiny_mce'%>
	<%= form.label :buy_now_price, "Cena kup-teraz :" %>
	<%= form.text_field :buy_now_price %>
	<%= form.label :cena_minimalna %><br/>
	<%= form.text_field :minimal_price %><br/>
	<%= form.hidden_field :user_attributes, :value => current_user.id%>
	<% unless @product.new_record? or @product.auction.new_record? %>
		<%= form.hidden_field :id, :index => nil%>
	<%end%>
<% end %>
<!-- form.hidden_field :auction_token, :value => "#{@auction.auction_token}" <br/> -->

<br/>

<br/>